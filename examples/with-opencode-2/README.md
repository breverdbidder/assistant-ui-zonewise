# with-opencode-2

Parallel Next.js example for the OpenCode runtime adapter that stays on top of
existing assistant-ui message primitives.

Set `NEXT_PUBLIC_OPENCODE_BASE_URL` if your OpenCode server is not running on
`http://localhost:4096`.
